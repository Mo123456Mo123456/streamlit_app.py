# 🚀 دليل البدء السريع

## التشغيل السريع (3 خطوات)

### Linux / macOS

```bash
# 1. امنح صلاحية التنفيذ للملف
chmod +x start.sh

# 2. شغل النظام
./start.sh
```

### Windows

```batch
# شغل الملف مباشرة
start.bat
```

## التشغيل اليدوي

### 1. تثبيت المتطلبات

```bash
pip install -r requirements.txt
```

### 2. تشغيل التطبيق

```bash
streamlit run ai_agent_app.py
```

### 3. افتح المتصفح

سيفتح التطبيق تلقائياً على:
```
http://localhost:8501
```

## أول استخدام

### 1. المحادثة الأولى

اذهب إلى صفحة "💬 المحادثة" واكتب:

```
مرحبا! ماذا يمكنك أن تفعل؟
```

### 2. جرب الأوامر السريعة

انقر على أي من الأزرار السريعة:
- 🔍 ابحث عن معلومة
- 📊 حلل بيانات
- 💻 اكتب كود
- 📋 ضع خطة

### 3. استكشف الوكلاء

اذهب إلى "🤖 الوكلاء" لترى جميع الوكلاء المتاحة وقدراتها.

## أمثلة سريعة

### بحث

```
ابحث لي عن معلومات حول الذكاء الاصطناعي
```

### تحليل بيانات

```
حلل البيانات التالية: [10, 20, 30, 40, 50]
```

### كتابة كود

```
اكتب لي كود بايثون لحساب الأعداد الأولية
```

### تخطيط

```
ضع لي خطة لتعلم البرمجة في 3 أشهر
```

## تشغيل الأمثلة

### الأمثلة الأساسية

```bash
python examples/basic_usage.py
```

### الأمثلة المتقدمة

```bash
python examples/advanced_usage.py
```

## تشغيل الاختبارات

```bash
python tests/test_agent.py
```

أو باستخدام pytest:

```bash
pytest tests/
```

## حل المشاكل الشائعة

### المشكلة: لا يعمل streamlit

**الحل:**
```bash
pip install --upgrade streamlit
```

### المشكلة: خطأ في الاستيراد

**الحل:**
```bash
# تأكد من أنك في المجلد الصحيح
cd /workspace

# أعد تثبيت المتطلبات
pip install -r requirements.txt --force-reinstall
```

### المشكلة: خطأ في قاعدة البيانات

**الحل:**
```bash
# احذف قاعدة البيانات وأعد البدء
rm ai_agent_knowledge.db
rm -rf database/
```

## الوصول من جهاز آخر

إذا أردت الوصول للنظام من جهاز آخر على نفس الشبكة:

```bash
streamlit run ai_agent_app.py --server.address 0.0.0.0
```

ثم افتح من الجهاز الآخر:
```
http://[IP-ADDRESS]:8501
```

## التخصيص

### تغيير إعدادات النظام

عدّل ملف `config.py`:

```python
# مثال: زيادة حجم الذاكرة
MAX_MEMORY_SIZE = 5000
```

### إضافة وكيل مخصص

انظر إلى `README_AI_AGENT.md` للتفاصيل الكاملة.

## المساعدة

للمزيد من المعلومات:
- اقرأ `README_AI_AGENT.md`
- شغل الأمثلة في مجلد `examples/`
- اطلع على الاختبارات في `tests/`

## الدعم

إذا واجهت أي مشكلة:
1. تحقق من وجود Python 3.8+
2. تحقق من تثبيت جميع المتطلبات
3. اطلع على رسائل الخطأ في Terminal

---

🎉 استمتع باستخدام نظام الوكيل الذكي!
